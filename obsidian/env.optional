# Absolute path to the Obsidian vault. OPTIONAL — when unset, the skill
# falls back to ~/Documents/Obsidian Vault. Declared here (not in
# env.required) so the Designer offers it as a non-mandatory binding and
# the launcher injects it into the sandbox when bound, WITHOUT blocking the
# run when it's empty.
OBSIDIAN_VAULT_PATH
