# env.required — none for the default CSV-export path.
# Sequences are written by the host agent; the lint + export scripts are stdlib Python.
# (If leads are read from / logged to a store, that store's creds become required for that
#  path — e.g. SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY — but the CSV path needs no key.)
