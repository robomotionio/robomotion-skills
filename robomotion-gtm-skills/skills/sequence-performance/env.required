# env.required — none.
# The CSV path (fetch_campaign.py --tool csv + analyze_sequence.py) is fully keyless, and
# the agent does reply classification / copy grading (no LLM key in the script layer).
