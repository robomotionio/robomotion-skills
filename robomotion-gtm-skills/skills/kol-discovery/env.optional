# env.optional — Apify LinkedIn posts-search actor (drives the post-data scoring half with
# accurate engagement metrics). Without it, the post-data half degrades to keyless serp
# (no engagement counts -> weak scoring; lean on web-research KOLs).
APIFY_API_TOKEN
