# env.required — vars without which the skill's scripts cannot run.
# None: the baseline engine (fetch_pages.py, fetch_feed.py, render_page.mjs, snapshot_store.py)
# is keyless and the keyless path costs $0. Synthesis is the agent.
