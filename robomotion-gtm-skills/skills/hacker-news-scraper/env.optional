# env.optional — only for persistent cross-run history/dedup in recurring monitor mode.
# Omit for one-shot searches (the script is stateless; dedup is orchestrated around it).
SUPABASE_URL
SUPABASE_KEY
