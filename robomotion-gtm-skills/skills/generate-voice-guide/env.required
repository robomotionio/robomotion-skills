# env.required — vars without which the skill's scripts cannot run.
# None: the analyzer is keyless and the paste-text corpus fallback needs no key.
# The host agent (not a script) authors the guide, so no LLM key here.
