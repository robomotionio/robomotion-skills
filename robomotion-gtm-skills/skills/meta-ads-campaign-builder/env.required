# env.required — vars without which the skill's scripts cannot run.
# None: this is a planning skill. The optional competitor Meta Ad Library scrape is keyless,
# and the plan/audience/copy/budget design is the host agent's reasoning.
