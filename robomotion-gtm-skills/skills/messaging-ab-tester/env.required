# env.required — vars without which the skill's scripts cannot run.
# None: the core generate-and-analyze loop is the host agent (variant synthesis + narrative)
# plus two stdlib-only scripts (schedule, scoring). No key is consumed by any script here.
# (The reference behavior ships as "no tools required" — the user deploys/reports manually.)
