# env.optional — enrichment only; the rule-based catalog runs without any of these.
# LLM keys are NOT used by the script (the host agent is the model). Listed only because
# the contract's topic clustering / funnel-stage inference is performed by the agent:
ANTHROPIC_API_KEY
OPENAI_API_KEY
# Hostile-site / sitemap-missing extractor fallback (agent-orchestrated):
APIFY_API_TOKEN
