# env.required — none. The pipeline runs keyless: luma_scrape_pw.mjs (Playwright direct
# event-page scrape) for extraction and slack_alert.py's --out-file for delivery. Apify and
# Slack are the recommended (optional) paths below.
