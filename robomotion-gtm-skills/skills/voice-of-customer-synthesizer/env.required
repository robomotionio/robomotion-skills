# env.required — none.
# normalize_feedback.py runs on keyless user-provided exports, and the agent does the
# sentiment/clustering/synthesis (no LLM key in the script layer). Review scraping is an
# optional enrichment (env.optional), not required.
