# env.required — vars without which the skill's scripts cannot run.
# None: Meta Ad Library + Google Transparency Center are public; the scrapers and the
# landing-page extractor are keyless. Hook clustering / funnel inference / teardown are
# the host agent's reasoning, not a scripted LLM call.
