# env.required — vars without which the skill's scripts cannot run.
# None: crawl_sitemap.py, cluster_url_patterns.py, serp_probe.py, and fetch_page.py are
# all keyless. Classification, template-quality scoring, and gap synthesis are done by the
# host agent (no LLM key in scripts).
