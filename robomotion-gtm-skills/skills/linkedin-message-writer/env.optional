# env.optional — only for the delegated research step via linkedin-profile-post-scraper.
# If set -> that skill's cookieless Apify actor; else -> its keyless Playwright li_at-cookie
# degrade, or write from user-supplied context only (no fresh research). The drafting/lint/
# export steps of THIS skill use no paid service.
APIFY_API_TOKEN
