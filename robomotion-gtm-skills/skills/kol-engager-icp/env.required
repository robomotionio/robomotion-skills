# env.required — none. The keyless degrade (extract_engagers.py --source playwright + an
# LI_AT session cookie, no paid API) is the fallback engager source, so no paid key is
# required. Apify/PhantomBuster are the recommended (optional) higher-volume paths below.
