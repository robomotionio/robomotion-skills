# env.optional — each gates only its own engine / enrichment; the skill runs without them.
# Additional answer engines (absent -> that engine is skipped, not a failure):
OPENAI_API_KEY
GEMINI_API_KEY
TAVILY_API_KEY
# Persist the run for later diffing (powers aeo-visibility-monitor); absent -> one-shot only:
SUPABASE_URL
SUPABASE_KEY
