# env.required — none.
# The default --tool generic_csv path exports a tool-ready import CSV with no key, so the
# skill always produces a usable result. The send/sequencing key is OPTIONAL: if set ->
# launch the campaign via the provider; else -> export CSV to send manually (see
# env.optional). Drafting is done by the host agent (no LLM key here).
