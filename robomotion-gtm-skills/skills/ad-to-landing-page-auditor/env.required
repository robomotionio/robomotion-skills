# env.required — vars without which the skill's scripts cannot run.
# None: the ad parser and the landing-page extractor are keyless (stdlib urllib).
# Message-match scoring, friction classification, and rewrites are the host agent's
# reasoning, not a scripted LLM call.
