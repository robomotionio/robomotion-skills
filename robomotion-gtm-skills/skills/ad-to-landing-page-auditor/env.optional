# env.optional — fallbacks only; the skill runs without these.
# Proxy for geo-matched LP fetch / escalated Playwright fetch (Robomotion Proxy):
HTTPS_PROXY
# Apify fetch for an LP behind hostile anti-bot. If set -> Apify fetch (last resort for
# heavy anti-bot); else -> keyless `fetch_landing_page.py` (urllib), escalating to the
# Playwright fetch + screenshot (default):
APIFY_API_TOKEN
