# env.required — vars without which the skill's scripts cannot run.
# None: all collectors (fetch_pages.py, fetch_feed.py, hn_fetch.py, wayback_fetch.py,
# render_page.mjs) and snapshot_store.py are keyless; durable state can be the local CSV
# store. Classification, reporting, and human checkpoints are the agent.
