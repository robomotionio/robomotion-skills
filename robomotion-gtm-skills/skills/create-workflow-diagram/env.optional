# env.optional — none gate the skill. LLM keys are NOT used by any script (the host agent
# is the model). Listed only because cleaning messy free-text into clean 2-4-word labels /
# icons / branch structure is performed by the agent; the regex parser works without it.
ANTHROPIC_API_KEY
OPENAI_API_KEY
