# env.required — vars without which the skill's scripts cannot run.
# None: this is a pure-reasoning composite. The host agent is the model; there is no script
# that makes an external or LLM call (the bundled intake.json is a static rubric asset).
