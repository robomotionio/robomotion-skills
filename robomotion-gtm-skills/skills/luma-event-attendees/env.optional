# env.optional — required for SEARCH mode and full guest-profile extraction (the Apify Luma
# actor). Without it, only direct-scrape (hosts + metadata, no full guest list) is available.
APIFY_API_TOKEN
# override the default Luma actor id
APIFY_LUMA_ACTOR
