# env.optional — primary cheapest people search; without it the skill degrades to the
# keyless serp_people.py LinkedIn-profile search.
APOLLO_API_KEY
# Deeper contact-discovery fallbacks (invoked via their own skills, not the bundled scripts):
DROPCONTACT_API_KEY
# + a LinkedIn session cookie, for employee-scrape depth
PHANTOMBUSTER_API_KEY
