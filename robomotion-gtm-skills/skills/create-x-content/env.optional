# env.optional — none.
# A missing voice guide degrades gracefully (prompt to generate, or warned-generic default)
# rather than hard-failing. The agent (not a script) does the drafting, so no LLM key here.
