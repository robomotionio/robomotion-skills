# env.required — vars without which the skill's scripts cannot run.
# None: serp Search/Auto-Complete + the bundled keyless scripts (load_keywords, fetch_pages)
# cover the core; the host agent is the synthesis engine.
