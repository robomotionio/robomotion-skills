# env.required — vars without which the skill's scripts cannot run.
# None: dns_scan.py (needs `dig` on PATH, no key) and source_inspect.py are keyless.
# Free mode (DNS + source + blacklists) produces a complete report. Synthesis is the agent.
