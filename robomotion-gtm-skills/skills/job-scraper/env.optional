# env.optional — Apify actors give structured fields (salary, seniority, full description).
# Without the token, jobs.py degrades to keyless site: search at reduced field richness.
APIFY_API_TOKEN
# override the default LinkedIn jobs actor id
APIFY_LINKEDIN_JOBS_ACTOR
# override the default Indeed actor id
APIFY_INDEED_JOBS_ACTOR
