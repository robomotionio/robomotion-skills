# env.required — none.
# fetch_url.py is keyless and the call-strategy synthesis is the agent's (no LLM key).
# CRM/outreach keys (env.optional) only gate the prior-interaction reconstruction; without
# them the timeline degrades to a cold meeting and web research still runs.
