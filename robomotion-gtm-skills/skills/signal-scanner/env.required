# env.required — none. Phase 1 runs keyless against a --snapshots TAM CSV/JSON export and
# writes to a workspace file; Phase 2 only computes scrape scope (the agent runs the
# engager/jobs sub-skills, each keyless-degradable). All paid services are optional below.
