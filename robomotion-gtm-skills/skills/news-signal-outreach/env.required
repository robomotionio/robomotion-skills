# env.required — none. Ingest is keyless; reasoning/drafting is the agent; and the launch
# step hands off to cold-email-outreach, which exports a CSV with no key. Every key below
# is OPTIONAL (see env.optional).
