# env.required — propagated from the AEO sub-skill the playbook orchestrates.
# Phase 1 runs aeo-visibility, whose query_engines.py hard-requires an answer-engine key.
# A sub-skill's hard requirement propagates to the playbook. Perplexity is canonical; any
# single answer-engine key the AEO sub-step uses satisfies this (else that sub-step exits).
PERPLEXITY_API_KEY
