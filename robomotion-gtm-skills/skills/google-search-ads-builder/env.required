# env.required — vars without which the skill's scripts cannot run.
# None: hn_terms.py (HN Algolia API), autocomplete_expand.py (Google Suggest), and
# build_import_csv.py are all keyless / stdlib. Architecture, copy, and bid strategy are
# the host agent's reasoning, not a scripted LLM call.
