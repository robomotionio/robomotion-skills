# env.required — none. The default is draft-only (categorize + draft, no send), and
# launch_campaign.py --tool generic_csv exports a tool-ready CSV with no key. Every key
# below is OPTIONAL (see env.optional).
