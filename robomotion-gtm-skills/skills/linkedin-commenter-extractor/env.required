# env.required — none. The script runs keyless via the bundled Playwright degrade
# (li_comments_playwright.mjs) using a LinkedIn `li_at` session cookie (LI_AT). The Apify
# actor and LI_AT cookie are both OPTIONAL (see env.optional); supply one of them to run.
