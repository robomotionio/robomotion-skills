# env.required — vars without which the skill's scripts cannot run.
# None: crawl_sitemap.py, cluster_url_patterns.py, expand_keywords.py, serp_probe.py, and
# fetch_page.py are all keyless. Pattern scoring and template design are done by the host
# agent (no LLM key in scripts).
