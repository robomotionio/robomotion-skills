# env.required — none.
# The keyless fallback (fetch_page.mjs — Playwright renders each review page and dumps its
# text) runs without any key, and the agent does the digest synthesis (no LLM key in the
# script layer). Apify is an optional upgrade for full, reliable extraction (see env.optional).
