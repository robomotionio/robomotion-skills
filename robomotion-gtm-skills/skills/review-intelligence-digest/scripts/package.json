{
  "name": "review-intelligence-digest-scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Playwright degrade-path fetcher for review-intelligence-digest (used only when APIFY_API_TOKEN is unavailable).",
  "dependencies": {
    "playwright": "^1.48.0"
  }
}
