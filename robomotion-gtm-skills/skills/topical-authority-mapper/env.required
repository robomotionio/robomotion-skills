# env.required — vars without which the skill's scripts cannot run.
# None: crawl_sitemap.py, expand_keywords.py, and serp_probe.py are all keyless
# (Google Autocomplete + public sitemap/RSS + DuckDuckGo HTML). Clustering, scoring,
# and calendar design are done by the host agent, not by a script — so no LLM key here.
