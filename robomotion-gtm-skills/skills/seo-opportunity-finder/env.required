# env.required — vars without which the skill's scripts cannot run.
# None: crawl_sitemap.py, expand_keywords.py, serp_probe.py, and fetch_page.py are all
# keyless. Gap classification, commercial-intent scoring, and synthesis are done by the
# host agent (no LLM key in scripts).
