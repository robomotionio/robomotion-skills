# env.optional — enrichment only; the skill runs fully (keyless) without these.
#
# Measured volume/difficulty + competitor DR/traffic via scripts/paid_seo.py (DataForSEO,
# HTTP Basic auth). When set, directional estimates upgrade to "measured"; absent ->
# directional from SERP probes (the adapter exits gracefully, keyless flow continues):
DATAFORSEO_LOGIN
DATAFORSEO_PASSWORD
# Alternative paid providers (NOT implemented in paid_seo.py; out-of-band only):
KEYWORD_ANALYSIS_API_KEY
SEMRUSH_API_KEY
AHREFS_API_TOKEN
SIMILARWEB_API_KEY
# Hostile-site catalog fallback (absent -> serp + sitemap crawl):
APIFY_API_TOKEN
