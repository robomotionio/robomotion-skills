# env.required — vars without which the skill's scripts cannot run.
# None: serp_probe.py (keyless SERP), render_page.mjs (SimilarWeb free page), and
# wayback_fetch.py are all keyless and produce a complete report. Synthesis is the agent.
