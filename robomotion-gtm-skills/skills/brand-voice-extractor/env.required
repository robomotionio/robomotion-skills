# env.required — vars without which the skill's scripts cannot run.
# None: page fetch is keyless (public HTTP), and the voice synthesis is done by the
# host agent (the agent is the model — no LLM key is consumed by any script here).
