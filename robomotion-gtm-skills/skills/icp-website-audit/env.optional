# env.optional — enrichment / hostile-site fallbacks only; the skill runs without these.
# Review-site scraping fallback when crawl/web-automation are blocked:
APIFY_API_TOKEN
# Semantic persona dedup (inherited from buyer-persona-generator; skip if absent):
PINECONE_API_KEY
QDRANT_URL
