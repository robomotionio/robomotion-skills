# env.required — none. Signal detection is keyless; qualify/personalize is the agent; and
# the launch step routes to cold-email-outreach / linkedin-outreach, both of which export a
# CSV with no key. Every key below is OPTIONAL (see env.optional).
