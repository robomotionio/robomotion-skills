# env.required — vars without which the skill's scripts cannot run.
# None: crawl_sitemap.py, fetch_page.py, and serp_probe.py are all keyless. Content
# classification, gap reasoning, brand-voice extraction, and synthesis are done by the
# host agent (no LLM key in scripts).
