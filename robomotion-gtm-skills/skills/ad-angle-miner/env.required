# env.required — vars without which the skill's scripts cannot run.
# None: public mining runs on web search + the keyless Meta Ad Library scraper.
# Angle extraction, scoring, and ranking are the host agent's reasoning, not a scripted
# LLM call.
