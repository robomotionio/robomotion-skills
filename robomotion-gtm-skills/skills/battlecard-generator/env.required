# env.required — vars without which the skill's scripts cannot run.
# None: fetch_pages.py and render_page.mjs are keyless; synthesis is the agent
# (platform-default model). The composite has no hard-gated credential.
