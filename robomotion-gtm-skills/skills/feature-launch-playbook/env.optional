# env.optional — only for downstream distribution the user opts into; none gate the core kit.
# One-off email announcement send:
SENDGRID_API_KEY
BREVO_API_KEY
# Delegated social/graphic sub-skills carry their own optional keys (voice-guide scraping,
# generative art) — see create-x-content / create-linkedin-content / graphics-studio.
APIFY_API_TOKEN
UNSPLASH_ACCESS_KEY
