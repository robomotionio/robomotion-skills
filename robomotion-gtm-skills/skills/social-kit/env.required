# env.required — vars without which the skill's scripts cannot run.
# None: init_run.py only scaffolds the run folder (keyless). The host agent drafts the copy
# and authors the graphic HTML; every sub-skill render/lint script is keyless.
