# env.optional — Apify LinkedIn jobs actor for clean structured output at scale.
# Without it, linkedin_jobs.py degrades to keyless site: search (shallower).
APIFY_API_TOKEN
# override the default LinkedIn jobs actor id
APIFY_LINKEDIN_JOBS_ACTOR
