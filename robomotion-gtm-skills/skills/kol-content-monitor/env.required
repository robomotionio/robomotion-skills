# env.required — vars without which the skill's scripts cannot run.
# None: both scrapers have keyless degrade paths and topic clustering/synthesis is the
# host agent's job (provider-swappable, no hard gate at the composite level).
