# env.required — vars without which the skill's scripts cannot run.
# Inbox access is inherent: no fallback (inbox access required). AGENTMAIL_API_KEY
# (env.optional) is an alternative inbox source, not a keyless substitute.
IMAP_HOST
IMAP_USER
IMAP_PASSWORD
