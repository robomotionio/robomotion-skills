# env.required — none.
# The default path is keyless: the script reads the watch page for the caption-track list
# and fetches YouTube's public `timedtext` track with stdlib urllib (no key, no cost).
# Apify is an optional upgrade for blocked cloud IPs (see env.optional), not required.
