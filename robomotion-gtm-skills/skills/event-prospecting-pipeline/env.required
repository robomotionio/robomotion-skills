# env.required — none at the playbook level. Gates live in the sub-skills it invokes
# (e.g. contact-cache requires SUPABASE_* for durable dedup; get-qualified-leads-from-luma
# requires APIFY_API_TOKEN). This playbook itself runs no gated scripts.
