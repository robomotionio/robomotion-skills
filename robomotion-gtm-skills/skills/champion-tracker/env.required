# env.required — none. The keyless degrade (pb_profiles_pw.mjs Playwright + an LI_AT session
# cookie, no paid API) is the fallback LinkedIn-profile source, so no paid key is required.
# PhantomBuster/Apify are the recommended (optional) higher-volume paths below.
