# env.required — none. The keyless degrade (pb_engagers_pw.mjs Playwright + an LI_AT session
# cookie, no paid API) is the fallback extraction source, so no paid key is required.
# Apify/PhantomBuster are the recommended (optional) higher-volume paths below.
# (search_pain_posts.py also degrades to a keyless web-search PLAN.)
