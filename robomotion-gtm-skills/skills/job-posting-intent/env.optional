# env.optional
# LinkedIn/Indeed job actors (structured, richer); estimate cost first
APIFY_API_TOKEN
# override the default LinkedIn jobs actor id
APIFY_LINKEDIN_JOBS_ACTOR
# override the default Indeed actor id
APIFY_INDEED_JOBS_ACTOR
# decision-maker suggestions via company-contact-finder
APOLLO_API_KEY
# only if LLM qualification is not platform-provided
ANTHROPIC_API_KEY
