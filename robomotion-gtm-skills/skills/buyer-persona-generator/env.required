# env.required — vars without which the skill's scripts cannot run.
# None: research_company.py fetches public pages with stdlib urllib (keyless), and
# persona synthesis is done by the host agent (no LLM key in the script layer).
