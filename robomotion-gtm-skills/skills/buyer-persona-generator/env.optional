# env.optional — enrichment / hostile-site fallbacks only; the skill runs without these.
# Review-site (G2/Capterra/TrustRadius) scraping fallback when direct fetch is blocked:
APIFY_API_TOKEN
# Semantic persona dedup (skip if absent):
PINECONE_API_KEY
QDRANT_URL
