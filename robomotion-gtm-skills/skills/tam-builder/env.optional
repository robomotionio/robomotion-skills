# env.optional — every paid service here has a keyless fallback; the skill runs without all.
# If APOLLO_API_KEY is set -> Apollo Company + People Search (full firmographics: employees,
# industry, funding stage — better quality). If not -> keyless serp_companies.py +
# apollo_watchlist.py serp degrade (blank firmographics).
APOLLO_API_KEY
# Durable TAM store enabling refresh tier-change detection and signal-scanner downstream.
# Degrades to a CSV-only single build without it.
SUPABASE_URL
SUPABASE_SERVICE_ROLE_KEY
# only if LLM-assisted fuzzy industry-fit scoring is used
ANTHROPIC_API_KEY
