# env.required — none hard-required by the scripts (github_profile.py + fetch_url.py are
# keyless). Calendar access is via the Google Calendar MCP (agent-authed, not an env var),
# and the brief writing is the agent's (no LLM key). Email delivery, when send_email is on,
# needs ONE of the channel keys in env.optional (gate: delivery is requested).
