# Internal integration token. Create an integration at
# https://www.notion.so/my-integrations and copy its key (starts with
# `ntn_` or `secret_`). You MUST also connect each target page/database
# to the integration in Notion, or the API returns 404 for it.
NOTION_API_KEY
