id: provider-asana
version: 1
entrypoint: plugin.py
permissions: [asana.read, asana.write]
router: "plugin:router"
heartbeat:
  - name: asana_inbox_poll
    interval_seconds: 300
    fn: poll_inbox
config:
  token_env: "ASANA_TOKEN"
  workspace_env: "ASANA_WORKSPACE"
