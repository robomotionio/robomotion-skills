# Changelog

All notable changes to the `referrals` skill.

## [1.0.0]

- Ported from [marketingskills](https://github.com/coreyhaines31/marketingskills) `referrals` (upstream version 2.0.0) into the Robomotion Hermes Agent skill format.
- Front-matter `tags` added; tool references rewritten to `${SHARED_DIR}`/`${SKILL_DIR}`.
- Tool credentials declared in `env.optional` (advisory; never blocks a run).
