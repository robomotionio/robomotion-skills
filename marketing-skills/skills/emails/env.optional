# env.optional — emails
#
# ALL OPTIONAL — gate-traced. Each integration CLI hard-gates on its own token
# (e.g. tools/clis/<tool>.js: `if (!TOKEN) { console.error('...required'); process.exit(1) }`),
# but this skill is knowledge that can use ANY of several alternative tools — or
# none at all. No single tool is a prerequisite (real fallback: a different tool,
# or pure-knowledge output), so every var below is OPTIONAL. The user supplies
# keys only for the tools they actually have.
# Tools this skill can use: activecampaign, audiencetap, beehiiv, brevo, customer-io, hubspot, kit, klaviyo, mailchimp, nitrosend, outreach, postmark, resend, salesforce, segment, sendgrid, shopify, zapier
ACTIVECAMPAIGN_API_KEY
ACTIVECAMPAIGN_API_URL
BEEHIIV_API_KEY
BREVO_API_KEY
CUSTOMERIO_API_KEY
CUSTOMERIO_APP_KEY
CUSTOMERIO_SITE_ID
KIT_API_KEY
KIT_API_SECRET
KLAVIYO_API_KEY
MAILCHIMP_API_KEY
OUTREACH_ACCESS_TOKEN
POSTMARK_API_KEY
RESEND_API_KEY
SEGMENT_ACCESS_TOKEN
SEGMENT_WRITE_KEY
SENDGRID_API_KEY
ZAPIER_API_KEY
