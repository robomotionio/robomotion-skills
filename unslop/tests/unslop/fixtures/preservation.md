# API Reference

The `fetchData()` function is key.

## Usage

Run `npm install` first. Then look at the examples at https://example.com/docs/api.

```python
# This code block must not be touched — including AI-isms like delve and tapestry.
def fetch_data():
    """Delve into the tapestry of data."""
    return "pivotal"
```

See [the full guide](https://example.com/guide) for a broad walkthrough. The short version is: call `fetch()` and handle errors. Retries are smooth.

    # Indented code block — also preserved.
    delve into tapestry

## Footer

It is essential to use the advanced patterns.
