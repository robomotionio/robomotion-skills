# Fine-grained personal access token with `issues:read` and `issues:write`
# scopes (and `metadata:read`). Generate at
# https://github.com/settings/tokens?type=beta. Repo-scoped tokens
# work for single-repo workflows; classic tokens with `repo` scope work
# for multi-repo.
GITHUB_TOKEN
