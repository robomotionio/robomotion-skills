# Personal Access Token (starts with `pat...`). Create at
# https://airtable.com/create/tokens with scopes data.records:read,
# data.records:write, schema.bases:read — and add each base you want to
# reach to the token's Access list (tokens are scoped per base).
AIRTABLE_API_KEY
