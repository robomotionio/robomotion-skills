# env.optional — research-keywords
# SerpAPI key for keyword-explorer.mjs / serp-analyzer.mjs. Both read
# `process.env.SERPAPI_KEY || ""` and fall back to free mode when unset,
# so this is optional — set it only to pull real Google autocomplete / PAA /
# SERP data via SerpAPI.
SERPAPI_KEY
