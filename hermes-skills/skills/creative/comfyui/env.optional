# Optional API key for Comfy Cloud. When absent, the skill can still drive
# a local ComfyUI server. Detected via variable indirection
# (`os.environ.get(ENV_API_KEY)` where ENV_API_KEY = "COMFY_CLOUD_API_KEY")
# in scripts/_common.py, which the regex-based detector skips.
COMFY_CLOUD_API_KEY
