# Personal access token used as the curl-auth fallback when neither
# `gh` CLI nor cached credentials are available. The script falls back
# gracefully to "none" if unset (shell `${VAR:-}` pattern), so it's
# optional. The regex-based detector misses the `${GITHUB_TOKEN:-}`
# form, so it's recorded here manually.
GITHUB_TOKEN
