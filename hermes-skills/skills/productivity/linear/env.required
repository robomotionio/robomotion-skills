# Linear personal API key. Read via `os.environ.get("LINEAR_API_KEY", "")`
# which the detector classified as optional, but scripts/linear_api.py
# does `if not key: sys.exit(2)` immediately after - so without it the
# script crashes. Effectively required.
LINEAR_API_KEY
